name: fly_conda_build_environment

channels:
  - defaults
  - conda-forge

dependencies:
  - python=3.9
  - setuptools
  - conda-build
  - anaconda-client
  - cudatoolkit
  - cupy
  - smina
  - pip
  - pip:
    - -r requirements.txt